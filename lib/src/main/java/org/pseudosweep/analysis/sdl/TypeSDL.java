package org.pseudosweep.analysis.sdl;

import org.pseudosweep.analysis.Type;

public enum TypeSDL implements Type {
    BREAK,
    CONTINUE,
    DECISION,
    DO,
    EXPRESSION,
    FOR,
    FOR_EACH,
    IF,
    INNER_CLASS,
    INNER_CLASS_RETURN,
    LAMBDA,
    LAMBDA_RETURN,
    RETURN,
    SWITCH,
    SWITCH_ENTRY_ASSIGNMENT,
    THROW,
    TRY,
    VARIABLE_DECLARATION,
    WHILE
}
